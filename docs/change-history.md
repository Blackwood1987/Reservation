# 변경 이력 및 전후 대비표

## 기록 원칙
- 대상: `main` 브랜치 푸시 또는 주요 기능 변경
- 형식: 반드시 `변경 전 / 변경 후 / 영향 범위`를 표로 기록
- 시점: 커밋/푸시 직전 문서 업데이트
- 검증: 최소 1개 검증 항목 기록 (예: `node --check app.js`)

## 템플릿
### YYYY-MM-DD | 버전/커밋
| 구분 | 변경 전 | 변경 후 | 영향 범위 |
|---|---|---|---|
| 기능 |  |  |  |
| UI/UX |  |  |  |
| 데이터/권한 |  |  |  |
| 운영 메모 |  |  |  |

검증:
- 

---

## 최신 변경

### 2026-03-03 | 모바일 예약관리/월간일정 UX 단순화
| 구분 | 변경 전 | 변경 후 | 영향 범위 |
|---|---|---|---|
| 기능 | 모바일 예약 카드에 예약 목록이 길게 노출 | 장비별 핵심 정보(현재/다음 예약, 가용 시점, 예약 건수) 중심으로 축약 | `renderScheduleMobile` |
| UI/UX | 예약 목록 전체 노출로 스캔 비용 증가 | 예약 프리뷰 2건 + `외 n건`으로 간결화 | 모바일 예약관리 |
| 기능 | 모바일 월간 일정 카드에 피크 정보까지 포함 | 예약 건수/가동률 중심으로 정보 밀도 축소(대기는 있을 때만 표시) | `renderCalendarMobile` |
| UI/UX | 모바일에서도 장비검색/필터초기화가 항상 노출 | 모바일에서는 장소/내예약 중심 필터만 노출, 보조 필터 숨김 | 예약관리 필터 |
| 운영 메모 | 모바일 화면에서 업무 핵심 정보 집중도 낮음 | 현장 작업자가 빠르게 예약 가능 시점 판단 가능 | 현장 모바일 사용성 |

검증:
- `node --check app.js`

### 2026-03-03 | 모바일 작업자 UX 단순화
| 구분 | 변경 전 | 변경 후 | 영향 범위 |
|---|---|---|---|
| 기능 | 모바일 대시보드에서 작업자도 요약/가동분석 영역이 함께 노출 | 작업자(및 게스트) 모바일에서는 요약/가동분석 숨김, 현재 상태 중심 구성 | `dashboard` 모바일 |
| UI/UX | 모바일에서 탭/패널 정보량 과다 | 현재 가동 현황 + 타임라인 + 상태 목록 중심으로 단순화 | `app.js`, `styles.css` |
| 기능 | 모바일 예약 카드에서 다음 사용 가능 시점 확인이 어려움 | 장비별 “사용 가능 시점/대기시간” 표시 추가 | `renderScheduleMobile` |
| 데이터/권한 | 상태 목록은 현재 상태만 표시 | 상태 목록에 “언제 사용 가능한지” 보조 텍스트 추가 | 작업자 모바일 운영 |
| 운영 메모 | 현장 작업자 관점에서 핵심 정보 집중도 부족 | 예약/현재 상태/가용시점 확인 흐름 강화 | 현장 모바일 사용성 |

검증:
- `node --check app.js`

### 2026-03-03 | 대시보드 하이라이트 UX 개선
| 구분 | 변경 전 | 변경 후 | 영향 범위 |
|---|---|---|---|
| 기능 | hover 이동 시 포커스가 즉시 해제되어 깜빡임 발생 가능 | 해제 지연(80ms) 적용으로 이동 중 깜빡임 완화 | 대시보드 룸맵/타임라인/상태목록 |
| UI/UX | 비포커스 항목 디밍 강도 과함(`opacity: 0.42`) | 디밍 완화(`opacity: 0.70`)로 자연스러운 시선 유도 | `styles.css` |
| 데이터/권한 | 포커스 변경마다 전체 노드 클래스 재토글 | 포커스 캐시 기반으로 이전/현재 중심 갱신 | `app.js` |
| 운영 메모 | 모바일 터치 환경에서도 동일 하이라이트 룰 적용 | `@media (hover: none)`에서 과한 하이라이트 제거 | 모바일 대시보드 |

검증:
- `node --check app.js`

### 2026-03-03 | 관리 3차 (운영 통계 확장)
| 구분 | 변경 전 | 변경 후 | 영향 범위 |
|---|---|---|---|
| 기능 | 운영 통계가 3개 카드(공정/유지보수/청소)만 표시 | 월 이동, KPI, 목적별 비율, 장소별 현황, 상위 사용 장비 추가 | 관리자 탭 `운영 통계` |
| UI/UX | 월 선택 제어 없음 | 이전/다음/현재 월 버튼으로 통계 월 이동 가능 | `app.html`, `styles.css` |
| 데이터/권한 | 캘린더 월 기준 집계에 종속 | 통계 전용 월 상태(`statsYear`, `statsMonth`)로 독립 집계 | `app.js` |
| 운영 메모 | 데모 통계 정보가 제한적 | 운영 리뷰 시 월간 근거 자료로 활용 가능 | 관리자 운영 |

검증:
- `node --check app.js`

### 2026-03-03 | bf479a7 (운영 이력/리포트 중심 전환)
| 구분 | 변경 전 | 변경 후 | 영향 범위 |
|---|---|---|---|
| 기능 | 승인 대기 중심 화면 | 운영 이력/리포트 중심 화면으로 전환 | 관리자 탭 `audit` |
| UI/UX | 대기 목록 단일 카드 | 이력 테이블 + 상태 요약 + 최근 관리자 작업 | `app.html`, `styles.css` |
| 데이터/권한 | 승인/반려 조작 UI 중심 | 삭제/반려 이력 조회 중심 구조로 정리 | `app.js` |
| 운영 메모 | 운영 근거 데이터 분산 | 이력 조회/인쇄/백업 흐름 일원화 | 운영/감사 |

검증:
- `node --check app.js`

---

## 백필(이전 주요 변경)

| 날짜(대략) | 커밋 | 핵심 변경 |
|---|---|---|
| 2026-01-29 | `195f40a` | 관리자 탭 필터 강화 및 리스크 가시성 개선 |
| 2026-01-29 | `17032af` | 로그인 폼 경고 수정, favicon 추가 |
| 2026-01-29 | `4d432e3` | 대시보드 가독성 및 모바일 포커스 UX 개선 |
| 2026-01-29 | `968e8bc` | 대시보드 요약/패널 UX 보강 |
| 2026-01-29 | `060dd7c` | 예약 흐름 안정화, 네트워크 대응 문서 추가 |
| 2026-01-29 | `bf583ba` | 작업자 예약 자동 확정, 09~18 시간 제한 |
| 2026-01-29 | `bd3364d` | 목적을 장비 단위로 스코프 분리 |
| 2026-01-29 | `851a359` | 관리자 목적 관리 기능 추가 |
| 2026-01-29 | `9e3aa77` | 룸맵/타임라인 스크롤바 강제 적용 |
| 2026-01-29 | `8144ea3` | 예약관리 화면 스크롤 처리 |
| 2026-01-29 | `602293c` | 일일 리포트 날짜 선택 인쇄 |
| 2026-01-29 | `cf2843c` | 반려 시 사유 입력 강제 |
| 2026-01-29 | `43053bd` | 관리자 삭제 시 사유 입력 강제 |
| 2026-01-29 | `467c160` | 자동 소독을 선택 옵션으로 변경 |
| 2026-01-29 | `649c8fa` | 장소/장비 설정 Firestore config 영속화 |
| 2026-01-29 | `f8c3bda` | 게스트/데모 접근 정리, 예약관리 장소 그룹화 |
| 2026-01-29 | `0a7ccd5` | Firebase Auth + 공유 예약 동기화 도입 |
| 2026-01-29 | `1a32140` | 회원가입/승인 흐름 도입 |
| 2026-01-29 | `f95bf4d` | 초기 데모 UI 구성 |
